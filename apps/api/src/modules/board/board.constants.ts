/**
 * Верхняя граница названия доски.
 *
 * Живёт отдельно от DTO, потому что нужна ДВУМ входам сразу — созданию и переименованию,
 * — а вторая копия числа означает, что однажды поправят только одну из них. Импортировать
 * константу из соседнего DTO было бы связью «вход зависит от другого входа», которой здесь
 * нет по смыслу: оба зависят от одного правила домена.
 *
 * Значение то же, что у displayName пользователя (100): предел не про UX, а про то, чтобы
 * в БД, логи и чужие интерфейсы не уезжал текст произвольного размера.
 */
export const BOARD_TITLE_MAX_LENGTH = 100;

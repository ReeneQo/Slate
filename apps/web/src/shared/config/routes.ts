/**
 * Реестр page-роутов приложения — единственный источник правды о путях.
 *
 * `as const` даёт литеральные типы (`'/login'`, а не `string`): опечатка в пути или ссылка на
 * несуществующий роут ловятся компилятором, а не пустым экраном в рантайме. Объект, а не класс —
 * пути это данные, а не поведение; классу тут нечего инкапсулировать.
 */
export const ROUTES = {
  home: '/',
  login: '/login',
  register: '/register',
} as const;

export type RoutePath = (typeof ROUTES)[keyof typeof ROUTES];

/**
 * Ключи кэша react-query для досок. Централизованы в одном объекте, а не разбросаны строками по
 * хукам: инвалидация после мутаций (`invalidateQueries({ queryKey: boardKeys.all })`) обязана
 * ссылаться РОВНО на тот ключ, которым запрашивался список. Строковый дубль в двух местах
 * однажды разъедется — и инвалидация тихо перестанет обновлять список.
 *
 * `as const` — ключ должен быть стабильным кортежем, react-query сравнивает его структурно.
 * Список сейчас плоский (без фильтров/пагинации — SLT-22), поэтому один ключ `['boards']`; когда
 * появятся параметры, тут вырастет фабрика `list(params)` — и снова в одном месте.
 */
export const boardKeys = {
  all: ['boards'] as const,
};

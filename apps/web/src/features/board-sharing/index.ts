/**
 * Публичная поверхность фичи board-sharing (SLT-43). Внешние слои (widgets) импортируют
 * `@/features/board-sharing`, а не внутренние файлы — раскладка (api/model/ui/lib) остаётся
 * деталью реализации.
 *
 * Наружу торчит только диалог: хуки/мутации/маппинг ошибок/доступ по роли — внутренняя механика.
 */
export { ShareDialog } from './ui/ShareDialog';

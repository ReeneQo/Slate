/**
 * Пути board-эндпоинтов — `as const` ВНУТРИ entities: сущность владеет своими путями (как auth
 * своими). Глобального файла эндпоинтов нет намеренно — он превратился бы в свалку путей всех
 * фич. Базовый префикс `/api` уже в клиенте (SLT-24), здесь только хвост.
 *
 * `byId` — функция-построитель, а не строка: путь удаления параметризован id. Объект с
 * построителем, а не класс — это по-прежнему данные, просто одно из значений вычисляется.
 */
export const BOARD_ENDPOINTS = {
  /** `GET /boards` (список) и `POST /boards` (создание) — один и тот же ресурс-коллекция. */
  root: '/boards',
  /** `DELETE /boards/:id`, `GET /boards/:id` — конкретная доска. */
  byId: (id: string): string => `/boards/${id}`,
} as const;

# @slate/shared-types

Общие типы между фронтом и бэком: элементы холста, DTO, контракты API.

Импорт: `import type { ... } from '@slate/shared-types';`

Без сборки — TS-исходники резолвятся напрямую внутри монорепо.

> Заглушка этапа 1. Типы добавляются вместе с соответствующими фичами.

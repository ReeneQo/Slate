/**
 * Верхняя граница названия доски.
 *
 * Живёт в общем пакете, а не рядом с DTO бэкенда, потому что ограничение нужно ТРЁМ местам
 * сразу: созданию доски, переименованию и форме на клиенте. Копия числа в любом из них однажды
 * разойдётся с остальными — и разойдётся молча: форма продолжит принимать 120 символов, а
 * сервер начнёт отвечать 400 на то, что клиент считал валидным.
 *
 * Значение то же, что у displayName пользователя (100): предел не про UX, а про то, чтобы в БД,
 * логи и чужие интерфейсы не уезжал текст произвольного размера.
 */
export const BOARD_TITLE_MAX_LENGTH = 100;
